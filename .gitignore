out
dist
node_modules
.vscode-test/
*.vsix

# Gemini Code Assist 缓存文件（如果有的话）
.gemini/cache/

# WXT 构建输出
.output/
.wxt/


# 环境配置文件
# 基础配置和环境特定配置提交到 Git
# 本地覆盖配置不提交
.env.local
.env.*.local

# 保留示例文件
!.env.local.example

# 注意：.env、.env.development 等文件应该提交到仓库
# 因为它们不包含敏感信息，只有默认配置
