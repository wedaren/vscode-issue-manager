---
# 生成大纲 Prompt 模板
---

请基于下面的标题和上下文，输出一个清晰、有序的章节大纲（只列出序号和章节标题，最多 12 项）。

- 标题：{{title}}
- 上下文：
```
{{selection}}
```

如果提供了本地参考文档（sources），请把每个参考文档的标题与片段附在上下文后，格式为：

```
参考文档：
{{sources}}
```

仅返回大纲，例如：
1. 背景
2. 问题要点
3. 解决方案

不要输出任何额外解释或说明。仅返回大纲条目。
